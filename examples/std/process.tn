-- Spawn a process and inspect its result shape.

import "process"

let result = process.run("echo", ["Tune"])

let command_ok(): Result<Bool, String> = {
  let completed: process.ProcessResult = process.run("echo", ["Tune"])!
  process.success(completed)
}
