-- Use floating-point helpers from the standard math module.

import "math"

let radius: Float = 3.0
let area: Float = math.pow(radius, 2.0) * 3.14159
let wave: Float = math.sin(0.0)
let bounded: Float = math.clamp(area, 0.0, 100.0)
