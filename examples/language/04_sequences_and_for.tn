-- Sequence literals, indexed access, and finite for.

let values = [1, 2, 3, 4, 5]

let sum(items): Int = {
  let total: Int = 0
  for item in items {
    total = total + item
  }
  total
}

let first: Int = values[0]
let checksum: Int = sum(values) + first
