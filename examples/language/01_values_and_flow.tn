-- Typed bindings, if expressions, interpolation, and boolean operators.

let score = 37
let passed = score > 30
let retry = not passed or score < 10

let status = if passed => "pass" else "retry"

let report = "{status}:{score}:{retry}"
print(report)
