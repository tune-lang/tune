# T1001: spawn task error

A spawned task or task join violated task execution rules.

Check task safety, captured state, host authorities, and whether the spawned expression can be scheduled by the active executor.

