# T1301: host authority denied

A host call requires an authority that the active profile did not grant.

Grant the required authority in the host profile or use an API available under the current profile.

