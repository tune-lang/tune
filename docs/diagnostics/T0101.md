# T0101: parse error

Tune could not read the source as valid syntax.

Check the highlighted token and the surrounding declaration boundary. Parser diagnostics should point at the first token where the source stopped matching Tune syntax.

