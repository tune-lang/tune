# T0401: numeric overflow

A numeric operation cannot be represented by its planned result shape.

Use a wider numeric shape, change the operation, or keep the value as a literal until a compatible context commits it.

