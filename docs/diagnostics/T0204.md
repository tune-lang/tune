# T0204: assignment shape mismatch

The assigned value cannot materialize into the target storage shape.

Tune commits storage meaning from annotations and constraints. Change the value, change the annotation, or introduce a new shadowing binding with the intended shape.

