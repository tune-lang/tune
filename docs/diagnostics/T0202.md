# T0202: duplicate name

Two declarations define the same name where Tune requires one meaning.

Move one declaration into a different scope or intentionally shadow through a local binding form that Tune allows.

